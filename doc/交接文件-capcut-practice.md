# CapCut 練習專案 — 交接文件

給下一個 session（不論是這台 PC 還是家用機）快速接續用。目標：不用重新摸索，直接從目前進度往下做。

**⚠️ 目前狀態（2026-08-08 第八次更新，家用機 user 帳號）：緊接第七輪，使用者要求把 14 段旁白文字（第七輪操作時講的旁白）實際加進 `ep1-demo-2` 這個 CapCut 專案本身，變成畫面字幕＋中文 TTS 語音，做成一支「有旁白的教學影片」。做法是：先把主視頻兩段素材用「時長」欄位直接拉長（0.2x／0.3x 速度）到總長 135 秒配合旁白總長，再依序對每段旁白新增文字圖層＋拖曳右邊界到目標時長＋套用「文字轉語音」。**目前只完成 4/14 段（第 0-3 段），第 5 段起（原本規劃批次處理 3 段一組）連續兩次批次呼叫中途失敗，兩次都意外點到「修改」按鈕彈出「專案設定」對話框，導致該批次的 3 段全部沒有真正建立**。因為同一個 session 已經連續執行極大量桌面自動化操作（週用量原本就已超額，這輪又新增上百次滑鼠鍵盤操作＋數十張截圖），判斷是自動化可靠度隨 session 拉長而下降（面板狀態不如預期時，固定座標點擊會點錯地方），已停止繼續批次嘗試，未匯出新檔案。詳見下方「## 第八輪」章節，內含已驗證可行的單段流程（供下個 session 逐段慢速接續）與兩次批次失敗的觀察。**

**⚠️ 舊狀態（2026-08-08 第七次更新，家用機 user 帳號）：緊接第六輪之後，使用者要求「重做一次同樣內容」，這次加碼「每個動作先文字顯示再 TTS 唸出再操作」的旁白流程。新草稿 `ep1-demo-2`，同樣 11 個技巧全部重做一輪，旁白用 edge-tts（zh-TW-HsiaoChenNeural）逐步生成 mp3 並用 ffplay 無視窗播放，再執行對應操作，已成功匯出 `ep1-demo-2.mp4`（121.2MB）。此輪額外**確認第六輪的 B-Roll 疊加軌道左鍵選取失效是可重現 bug**（不是偶發），連續兩輪、兩個不同疊加片段都命中同樣狀況。詳見下方「## 第七輪」章節。**

**⚠️ 舊狀態（2026-08-08 第六次更新，家用機 user 帳號）：新開一輪「EP1 教學逐步復刻」demo（草稿 `ep1-demo`），依照 YouTube 影片《免費影片剪輯軟體...【剪映教學分享EP1】》(<https://youtu.be/Ke43EELMDlM>) 的內容順序，逐一操作剪映桌面版並截圖驗證。11/13 個技巧完成且已實際匯出成功（`ep1-demo.mp4`，121.2MB）。詳見下方「## 第六輪」章節，內含一個新發現的踩坑（新建的疊加軌道左鍵點擊選取失效）。**

**⚠️ 舊狀態（2026-08-07 第五次更新，公司機 B00332）：Stage 3「有聲素材」卡點已用替代方案解掉（見下方「## 第五輪」章節）——CapCut 免費資料庫的素材本質上全無內嵌語音，改用內建「文字轉語音」功能生成真人語音再測自動字幕，可行且效果良好。這輪在新草稿 `addwii-demo` 做了 `doc/練習題-仿製addwii短片.md` 的 Lesson 7.1（找有聲素材，結論是資料庫沒有）+ 7.4 前半（TTS + 自動字幕 + 字幕樣式）+ 部分 7.6（背景音樂），因 session 用量已逼近上限提前收工，Hook（7.2）/B-roll 分鏡（7.3）/品牌收尾（7.5）/完整匯出（7.6 後半）尚未做，下一個 session 可直接接續 `addwii-demo` 草稿或參考本次發現另開新草稿。**

**⚠️ 舊狀態（2026-08-07 第三次更新，公司機 B00332）：第一輪（24 堂課）已全數完成並保留不動；第二輪（草稿 `0807-R2`，存在家用機 user 帳號，這台公司機沒有這份草稿）Stage 1-2（8 堂課）已完成，卡在 Stage 3。第三輪（公司機 B00332）與第四輪（`0807-R3`）都在找有聲素材時中止，見下方對應章節——這些發現已被第五輪的結論取代（資料庫素材確認全無聲，不用再繼續海底撈針）。**

- 第一輪完整紀錄：見下方「Stage 6 完成紀錄」與更早的 Stage 0-5 表格，**不要刪改**，草稿 `0806`（公司機 B00332）與 `0807`（家用機）都原封不動保留供對照。
- 第二輪進度與起手 Prompt：見文件最下方「## 第二輪（Pro 素材重做）」章節裡的「第二輪進度紀錄」和對應的起手 Prompt，**下一個 session 直接跳去那一段開始**，不用重跑 Stage 1-2。

## 這個專案是什麼

Kindle 書 44《剪映：剪出新視野》的視覺化＋實作練習專案。

- 本 repo（`kindle-44-剪映`）原本是 `book-viz-pack` skill 產出的 5 張心智圖 + PPTX 合輯，後來擴充成「CapCut 實戰特訓」：
  - `mermaid/剪映/` — 5 張心智圖 `.mmd`/`.png` + 圖表合輯 PPTX
  - `slide-deck/` — 15 章節投影片，**副檔名是 `.pptx` 但實際內容是 PDF**（NotebookLM harvest 腳本命名遺留問題），要用 PyMuPDF（`fitz`）開，不能用 `python-pptx`
  - `CLAUDE.md` — 專案架構說明
- 課程 Artifact（24 堂互動課程 + 進度追蹤 + 實測踩坑筆記）：
  **<https://claude.ai/code/artifact/fc20a1d8-c2c7-4d63-978a-bfa22c6522b1>**
  這是把 15 章內容改編成 CapCut Web/桌面版的 7 階段 24 堂練習，網頁本身是深色「調色室」風格，用 Big Shoulders Display + JetBrains Mono 字體（base64 內嵌），進度存在瀏覽器 localStorage（換瀏覽器/PC 不會保留，要手動重新勾）。

## 帳號 / 訂閱狀態（截至本次收工）

- CapCut 帳號：已在 capcut.com 用註冊完成（Google / email，視當初操作而定，實際帳號資訊使用者自行確認）
- **CapCut Pro：已購買**（桌面版右上角會顯示「Pro」徽章，帳號綁定不是機器綁定，換 PC 登入同帳號應直接生效）
- 桌面版：已透過 `winget install --id ByteDance.CapCut -e --accept-package-agreements --accept-source-agreements` 裝在**這台**公司機（B00332）。換 PC 要重裝，見下方「換新 PC 時」章節。

## 完整進度總表：Stage 0-5 已完成（22 堂課）

在 CapCut 桌面版建了一個練習專案（名稱 `0806`，9:16 比例），2026-08-06 這天連續完成 Stage 0 到 Stage 5：

| Stage | Lesson | 做了什麼 | 結果 |
| --- | --- | --- | --- |
| 0 | 帳號與環境 | 註冊 CapCut Web 帳號、認識工作區 | 完成 |
| 1.1 | 建立專案 + 設定比例 | 新建專案，長寬比例 原圖→9:16 | 完成 |
| 1.2 | 匯入素材 | 從內建免版稅資料庫加入素材 | 完成 |
| 1.3 | 時間軸手感 | （示範於分割步驟中一併完成） | 完成 |
| 1.4 | 分割與無縫刪除 | 00:00:14:26 處分割，刪除右段確認 ripple delete（30s→14:26 無縫接合） | 完成 |
| 1.5 | 匯出第一支測試片 | 踩到 Pro 限制 + 版權「未編輯」限制兩個坑，換成免費素材+加濾鏡/文字後成功匯出 1080P mp4 | 完成 |
| 2.1 | 精準剪輯點微調 | 方向鍵逐格移動播放頭（00:05:17→00:05:22，5 幀） | 完成 |
| 2.2 | 素材屬性調整 | 速度 1.00x→1.50x（時長 15.4s→10.3s），套用「漸隱閉幕」退場動畫做淡出 | 完成 |
| 2.3 | 多軌排版做子母畫面 | 免費素材拖到新軌、與主素材時間重疊、縮放 40%、Y 位移 -650 | 完成 |
| 3.1 | 音訊基礎：分離、降噪、淡入淡出 | 「擷取音訊」呈灰階不可點擊（無人聲音軌），改用「新增音樂」加入 Magic Piano 背景樂，裁剪至 ~14s，套用淡入 1.0s、淡出 1.0s、降噪 | ✅ 改用背景音樂示範 |
| 3.2 | 節奏踩點 | 選取音樂軌 → 工具列「自動標記節拍」→ 節拍1 → 時間軸出現橘色節拍點，在 00:00:05:22、00:00:07:09 兩個節拍點對主視頻素材分割 | ✅ 完全依課程步驟 |
| 3.3 | 自動字幕與文稿校對 | 「產生字幕」灰階、自動字幕面板點「產生」無輸出（無語音可辨識），改用「新增文字」手動輸入「字幕樣式示範」，套用花字樣式（字體+黑色描邊+底色一次到位） | ✅ 改用花字樣式示範 |
| 3.4 | 花字與貼紙動畫 | 文字套用進場動畫「鏡空打字機」0.5s + 退場動畫「漸隱閉幕」0.5s；新增星星貼紙 + 套用「彈跳入口」進場動畫 | ✅ 完全依課程步驟 |
| 4.1 | 特效庫與轉場比較 | 對第一段素材套用「泡泡」粒子特效；在兩個節拍分割點分別加「疊化」轉場(A) 與「快速滑動」轉場(B)，可比較節奏差異 | ✅ 完全依課程步驟 |
| 4.2 | 濾鏡與調節 | 套用「加州日光」濾鏡並把強度調到 70；切到調整面板手動設曝光 12/對比度 10/飽和度 15；隱藏濾鏡軌（眼睛圖示）只留手動調節做比較 | ✅ 完全依課程步驟 |
| 4.3 | LUT 調色 | LUT 名稱下拉選單只有「無」（無內建清單），改用「自動調整」（強度滑桿預設 80→調到 60）示範，開關切換比較 | ✅ 改用替代方案 |
| 4.4 | 蒙版去背合成 | 蒙版「線性」圖示點擊/雙擊皆無反應（功能故障），跳過角度/羽化；改為搜尋人像素材加入新軌，套用「移除背景→自動移除」，AI 處理後確認去背成功（透明背景讓下層字幕透出） | ✅ 部分完成（去背核心技能達成，蒙版跳過） |
| 5.1 | 關鍵幀原理：基本運鏡 | 對第一段素材「縮放」屬性打兩個關鍵幀：片頭 167%、片尾 190%（放大 23%），畫面平滑放大 | ✅ 完全依課程步驟 |
| 5.2 | 進階組合：文字消散效果 | 對字幕文字疊加兩組關鍵幀：透明度 100%→0%（片頭→片尾）+ 位置 Y 0→-40（輕微上飄），兩組一起播放做出消散飄散效果 | ✅ 完全依課程步驟 |
| 5.3 | 動態運鏡挑戰 | 對人像素材手動在 4 個時間點（00:08/00:10/00:11/00:13）打「位置」X 關鍵幀（0→35→-25→50）模擬人物移動運鏡 | ✅ 完全依課程步驟 |

**草稿本機路徑**（僅供除錯參考，換 PC 不會自動出現）：
`C:\Users\B00332\AppData\Local\CapCut\User Data\Projects\com.lveditor.draft\0806`

**目前 `0806` 專案時間軸結構**（共 6 軌，由上到下）：

1. 人像素材軌（Stage 4.4/5.3 新增）：一段免版稅人像影片，已套用「自動移除」去背，並打了 4 個位置關鍵幀
2. 特效軌：泡泡粒子特效（Stage 4.1）
3. 貼圖軌：星星貼紙 + 彈跳入口動畫（Stage 3.4）
4. 濾鏡軌：加州日光濾鏡，強度 70（Stage 4.2；目前眼睛圖示為隱藏狀態，因 4.2 步驟 3 要求關閉濾鏡比較）
5. 文字軌：「字幕樣式示範」，套用花字樣式 + 進場/退場動畫 + 透明度/位置關鍵幀（Stage 3.3/3.4/5.2）
6. 主視頻軌：3 段免版稅素材（因 Stage 3.2 節拍分割），套用縮放關鍵幀（Stage 5.1）、疊化/快速滑動兩個轉場（Stage 4.1）、手動曝光/對比/飽和度調整（Stage 4.2）
7. 音訊軌：Magic Piano 背景音樂 ~14s，淡入淡出 1.0s + 降噪（Stage 3.1）

## ⚠️ 踩坑總表：三個 CapCut 桌面版真實限制（Stage 3-4 發現）

1. **練習素材完全無語音音軌**（Stage 3 連續三堂課命中）：`0806` 專案主視頻用的是 CapCut 內建免版稅素材（"Used Damaged Notebook Schoolwork Paper Template Background"），完全沒有人聲/語音音軌。導致：
   - **擷取音訊**（右鍵選單）→ 灰階不可點擊
   - **產生字幕/逐字稿**（右鍵選單 + 文字分頁自動字幕面板）→ 面板可開、按鈕可點，但點「產生」後沒有任何輸出（沒報錯，就是靜默無結果）
   - 對應地，**降噪**這類「音軌屬性」功能不受影響（背景音樂軌本身就有降噪開關，跟人聲無關），可正常使用
   - **因應方式**：直接說明這是素材限制，改用功能對等的替代操作示範（背景音樂軌代替人聲軌、手動文字代替語音辨識文字），不必真的去找/錄一段有人聲的素材
2. **LUT 沒有內建清單，只能匯入 .cube/.3dl 檔案**：影片素材「調整」面板的 LUT 名稱下拉選單只有「無」一個選項；左側導覽列「調整」大類下的 LUT 分頁也只有「+匯入 CUBE 和 3DL 檔案」，沒有任何可直接選用的內建 LUT 素材庫（跟濾鏡/特效/轉場有完整可拖拉素材庫的行為不同）。**因應**：改用「自動調整」（AI 一鍵校色，同樣有強度滑桿）示範一鍵套色+找自然感的技能。
3. **「遮罩轉場」面板的蒙版形狀圖示（線性/鏡面/圓形/矩形等）完全無回應**：位置在影片素材「調整」面板 → 最後一個子分頁「遮罩轉場」→ 上方「遮罩」勾選框打勾後，下方會出現形狀選單，但點擊/雙擊任一形狀圖示（包含課程要求的「線性」）畫面與預覽都毫無變化，沒有出現角度/羽化滑桿。已排除選錯圖示（用截圖放大確認過座標），判斷是這個版本的真實功能故障或需要特定前置條件才能啟用。**因應**：此步驟直接記錄限制、跳過角度/羽化練習，把重心放在「去背」（Remove Background → 自動移除）上，這個功能正常可用。

**後續 Stage 若又遇到類似限制**（功能灰階/無回應/無內建素材），直接照上面的因應模式處理：記錄限制 → 找功能對等的替代操作 → 繼續往下做，不要卡在同一個功能上反覆排查超過 2-3 次嘗試。

## Stage 6 完成紀錄（2026-08-07，家用機 user 帳號）

這台家用機第一次執行本專案，CapCut 桌面版尚未安裝，流程：`winget install --id ByteDance.CapCut` 裝好 → 啟動確認已登入（楊政憲 / 2820807665）、Pro 續訂日期 2026/08/12 直接生效（帳號綁定，不用重買）→ 新建練習專案 `0807`（9:16，30fps）→ 依「換新 PC 啟動步驟」選項 1（不補做 Stage 0-5，直接進 Stage 6，因為 6.1 本質是驗收整合）。

`0807` 專案素材：CapCut 內建免版稅素材庫的 "Kusatsu onsen hot spring"（24 秒海洋空拍）+ Magic Piano 背景音樂，時間軸 3 軌（1 主視頻／1 文字／1 音樂）。

| Stage | Lesson | 做了什麼 | 結果 |
| --- | --- | --- | --- |
| 6.1 | 命題實作：12 大特效技法擇一 | 主題「夏日運鏡+踩點」：背景音樂做「自動標記節拍」→ 節拍1，在主視頻節拍點分割成 3 段；第 1、2 段之間套用「疊化」轉場（0.5s）；第 3 段打縮放關鍵幀 100%→130%（運鏡放大）；加入文字「夏日清涼一夏」，套用花字（紅底黑框）+ 進場動畫「復古打字機」+ 退場動畫「漸隱閉幕」。整合踩點/轉場/關鍵幀/花字四項技巧於一支 24 秒短片 | ✅ 完全依課程步驟，四項技巧全部到位 |
| 6.2 | 正式導出設定 | 匯出面板比較 30fps vs 60fps：**檔案大小都約 37MB，幾乎沒差**（因為位元率設「推薦」是內容自適應動態計算，不是隨 fps 固定倍增；且來源素材本身是 30fps，60fps 只是補幀不會增加真實資訊量）→ 維持 30fps（比對來源幀率無意義升到 60fps）→ 1080P / H.264 / mp4，9:16 比例已符合 IG Reels / YouTube Shorts 直式規格 → 實際執行匯出成功，檔案存在 `C:\Users\user\AppData\Local\CapCut\Videos\0807.mp4`（16.9MB / 25 秒） | ✅ 完全依課程步驟，且額外驗證了「fps 對檔案大小影響 ≠ 直覺倍增」這個關鍵認知 |
| 6.3 | 素材管理與疑難排解 | 三個子任務：**(1) 觸發素材遺失** — 手動把 `Kusatsu onsen hot spring` 對應的快取檔（`User Data/Cache/onlineMaterial/<hash>.mp4`）改名模擬遺失，重開專案後 CapCut **沒有跳出「重新連結」提示**，而是自動從雲端重新抓取了同名快取檔（改回原檔名時系統回報「檔案已存在」，證實真的被重新下載）。**(2) SRT 字幕** — 對本專案跑「自動字幕→產生」，結果與 Stage 3.3 一致：進度跑到 100% 但沒有任何字幕輸出（背景音樂無人聲，無內容可辨識）。**(3) 雲端同步** — 首頁「專案」清單右上角有「專案同步」按鈕，點開顯示「開啟專案同步」提示，**預設是關閉的**、需要使用者手動按「開啟」才會啟用跨裝置同步；本次刻意不啟用（帳號設定變更需另外詢問），只確認了功能位置與預設狀態 | ✅ 三個子任務都完成，且都得到具體、可驗證的真實結果（不是紙上談兵） |

**Stage 6 三個關鍵發現（供未來查閱）：**

1. **CapCut 內建免版稅素材有雲端備援，本機匯入的檔案沒有**：素材庫（資料庫）拖進時間軸的素材是走「線上素材」機制、有雲端 ID，本機快取檔被搬移/砍掉後重開專案會自動重新下載，不會觸發「重新連結」提示。「重新連結」功能是給真正使用者自己匯入（如桌面上的照片/影片）的本機檔案用的——這類檔案若被移走，才會在重開草稿時看到遺失提示與重新連結入口。
2. **匯出面板的幀率選項不是「越高清晰度越好」的簡單線性關係**：30fps 素材硬升到 60fps，CapCut 用「推薦」位元率模式估算出的檔案大小幾乎不變（都約 37MB），因為位元率是依內容動態決定，不是隨 fps 固定倍增；來源本來就是 30fps 的素材升 60fps 也不會生出更多真實畫面資訊，純粹是補幀，通常不必要。
3. **CapCut 桌面版的雲端「專案同步」預設關閉**：不會自動幫你把草稿同步到雲端/手機，要在首頁「專案」清單的「專案同步」按鈕手動開啟才會生效。這代表換到另一台沒同步過的機器，草稿確實不會自動出現——交接文件前面「換新 PC 時」章節建議的「不補做舊 Stage、直接新建精簡專案」策略，其實正好對應這個限制的正確因應方式。

## 24 堂課程全部完成 — 總結

Kindle 書 44《剪映：剪出新視野》CapCut 實戰特訓，7 階段 24 堂課全部 live demo 完成：

- **Stage 0**（帳號與環境）→ **Stage 5**（關鍵幀設計）：2026-08-06 於公司機 B00332，草稿 `0806`，22 堂課
- **Stage 6**（綜合實戰與正式輸出）：2026-08-07 於家用機 user 帳號，草稿 `0807`，3 堂課

兩台機器分別建立獨立草稿（草稿不跨機同步，見上方發現 3），但涵蓋的知識點完整銜接：從帳號註冊、剪輯基本功、音訊/字幕、特效/轉場/濾鏡、關鍵幀運鏡，一路做到綜合命題實作、正式導出與素材管理疑難排解，24 堂課對應書中全部相關章節（第 1 章～第 22 章）都有對應的手作練習或明確記錄的功能限制。

全程累積 3 個「CapCut 桌面版真實限制」發現（Stage 3-4：練習素材無語音、LUT 無內建清單、遮罩轉場圖示無回應）+ 3 個 Stage 6 發現（內建素材雲端備援、fps 對檔案大小非線性、雲端同步預設關閉），都已記錄在本文件供未來查閱或用於製作教學內容。

## 換新 PC（家用機／另一台公司機）時的啟動步驟

1. `git pull` 這個 repo，確認 `CLAUDE.md`、`slide-deck/`、`mermaid/剪映/`、`doc/交接文件-capcut-practice.md` 都在且是最新版
2. 安裝 CapCut 桌面版：

   ```bash
   winget install --id ByteDance.CapCut -e --accept-package-agreements --accept-source-agreements
   ```

3. 啟動 CapCut，用同一組帳號登入 → 因為 Pro 是綁帳號不是綁機器，登入後應該直接生效，不用重買
4. **草稿不會跨機同步**（除非有開草稿雲端備份且該素材有上傳）。新 PC 上兩個選項：
   - 直接跳去做 Stage 6，不補做 Stage 0-5（推薦，Stage 0-5 已經在這份文件記錄完成，course artifact 上也可以手動勾選，且 Stage 6.1 本質是「驗收整合」不一定要有實體草稿）
   - 想要有實體草稿可以操作的話，花 10-15 分鐘照 Stage 1.1-1.2 重新建一個空專案＋丟幾個免費素材進去，模擬 `0806` 現有的軌道結構（可參考上方「目前 `0806` 專案時間軸結構」章節），不用重跑整個 demo
5. 打開課程 Artifact 網址，把 Stage 0-5 的 lesson checkbox 手動勾起來（localStorage 不跨機，這步是必要的）

## 給 Claude 的操作要點（桌面版自動化技術細節）

沿用 `~/.claude/instructions/windows-desktop-automation.md` 的 pywin32 + pywinauto 套路，但 CapCut 桌面版是 canvas 渲染（UIAutomation 抓不到細節元件），實際上是用「螢幕座標點擊」而非 accessibility tree，額外要注意：

- **DPI 感知必開**：多螢幕環境下，Python 若沒呼叫 `ctypes.windll.shcore.SetProcessDpiAwareness(2)`，`win32gui.GetWindowRect()` 回傳的座標會跟 `ImageGrab` 實際截到的像素對不上（座標會隨著滑鼠最後停留的螢幕 DPI 而漂移）。**每個腳本開頭都要設**。
- **截圖用 `PIL.ImageGrab.grab(bbox=rect, all_screens=True)`**，不要用 `pyautogui.screenshot()`（後者只截主螢幕，多螢幕環境下 CapCut 視窗如果在副螢幕會整張黑圖）。
- **CapCut 會開很多個同名「CapCut」隱藏視窗**（helper process），篩選時用 `win32gui.IsWindowVisible(hwnd) and rect 面積夠大` 排除雜訊，主編輯器窗通常是最大的那個 visible window。
- **素材庫 Pro 判斷**：縮圖左上角有紫色鑽石標記 = Pro 專屬素材，免費帳號可預覽/拖進時間軸但無法匯出（本 PC 已買 Pro，不用再迴避，但换到未購買 Pro 的帳號上要記得繞開）。
- **匯出前置條件**：即使素材免費，完全沒編輯過也無法匯出（版權保護機制），至少要加一個濾鏡或文字圖層才會被判定「已編輯」。
- **多軌 PIP 技巧**：新素材預設會接在同軌尾端，要疊成子母畫面必須拖到「時間上與既有素材重疊 + 垂直方向的空白軌道區」，系統才會自動建新軌。
- **CJK 文字輸入**：`pyautogui.typewrite()` 打繁體中文字會變亂碼（掃描碼機制，跟 `windows-desktop-automation.md` 記錄的 Telegram 案例同一個成因）。如果要打乾淨的中文字，改用剪貼簿貼上法（`Set-Clipboard` + `Ctrl+V`），不要用模擬鍵盤逐字打。
- **素材庫「先選再加」兩段式互動**：音樂/特效/濾鏡/貼圖/文字這些素材庫項目，第一次點擊只會選取+預覽（右側面板切成專案總覽），要再點一次同一個縮圖才會冒出「+」加入按鈕，點「+」才真的加進時間軸。單純雙擊不一定等同兩次單擊，遇到「選了但沒加入時間軸」先試單擊兩次。
- **拖曳素材到時間軸空白處＝強制建新軌**：直接點「+」或雙擊縮圖，素材可能被插入目前選取中的既有軌道（把原本素材往後推），不是疊加。要疊加成新的獨立軌道（如疊人像、疊貼圖），改用滑鼠從素材庫縮圖「拖曳」到時間軸最上方的空白區域。
- **工具列圖示 tooltip 常抓不到**：`SetCursorPos` 純位移不一定觸發原生 hover 事件，tooltip 有時完全不跳出來。與其花時間湊 tooltip，不如直接點下去看跳出什麼面板/對話框更快。
- **LUT 只能匯入，沒有內建清單**；**「遮罩轉場」面板的蒙版形狀圖示會完全無回應**——這兩個功能遇到直接照上方踩坑總表寫的方式跳過/替代，不用重複排查。
- **關鍵幀操作三步驟**（Stage 5 新學到，很重要）：
  1. 選取素材 → 找到要做動畫的屬性（縮放/位置/旋轉/不透明度），這些屬性平常收在對應面板（影片素材在「基礎→轉換」、文字素材在「基礎→轉換」+「不透明度」）
  2. **滑鼠移到該屬性那一列上 hover**，數值框右側才會浮現「‹ ◇ ›」關鍵幀控制列（不 hover 完全看不到，容易誤以為沒有這個功能）
  3. 把播放頭移到要打關鍵幀的時間點 → 精準點擊中間的 **◇ 鑽石本體**（不是滑桿、不是數值框——點太靠左會誤觸滑桿把數值拖走，要點鑽石圖示的正中心）→ 鑽石會變成填滿的**青色實心菱形**確認新增成功；之後只要移動播放頭到別的時間點、直接改數值，就會自動在新位置新增下一個關鍵幀（不用每次都點一次鑽石）
  4. 時間軸上該素材的頂端會出現一整排小白點/鑽石標記，對應每個關鍵幀的位置，可以用來視覺化確認打了幾個
- **Telegram 桌面版通知可能突然搶走視窗焦點、整個蓋住 CapCut**（本次 session 實測踩到）：跳出通知時前景視窗會變成 Telegram，`ImageGrab.grab(bbox=capcut_rect)` 這時候截到的是 Telegram 的畫面內容，不是 CapCut。**處理方式**：
  1. 絕對不要對著看起來像 CapCut 位置的畫面盲點——先截圖確認目前抓到的到底是誰的視窗
  2. 用 `win32gui.EnumWindows` 重新枚舉，確認 CapCut 的 hwnd 是否還存在（通常還在，只是被蓋住，沒有真的關閉）
  3. 用 `win32gui.ShowWindow(telegram_hwnd, win32con.SW_MINIMIZE)` 把 Telegram 縮到最小（用 API 縮小，不要點擊 Telegram 內容本身，避免誤觸私人/公司對話裡的按鈕）
  4. 再用 `AttachThreadInput` + `SetForegroundWindow` 把 CapCut 拉回前景（單純 `SetForegroundWindow` 常因 Windows 焦點鎖定機制失敗，要搭配 `AttachThreadInput`，寫法見 `windows-desktop-automation.md`）
  5. 這類通知彈出是不可預期的，之後每次連續截圖操作前，養成習慣先看一眼截圖內容是否真的是 CapCut 介面，不要照座標無腦連續點擊

## 相關連結

- 課程 Artifact：<https://claude.ai/code/artifact/fc20a1d8-c2c7-4d63-978a-bfa22c6522b1>
- GitHub repo：<https://github.com/chenghyang2001/kindle-44-jianying-viz>
- CapCut 官網：<https://www.capcut.com>

## 起手 Prompt（歷史紀錄 — 24 堂課已全部完成，以下留存供未來類似專案參考格式）

在任何一台已經 `git clone`／`git pull` 過這個 repo 的 PC 上，用 Claude Code CLI 在專案目錄下開新 session，貼這段：

```
讀 doc/交接文件-capcut-practice.md，接續 CapCut 練習專案。
Stage 0-5（22 堂課）已經 live demo 完成，現在要繼續並完成最後的 Stage 6
（綜合實戰與正式輸出，Lesson 6.1-6.3：12 大特效技法命題實作、正式導出設定、素材管理疑難排解）。
如果這台 PC 還沒裝 CapCut 桌面版，先幫我裝好、開好、確認 Pro 帳號登入狀態；
如果已經裝好，直接開啟或新建一個練習專案（草稿不會跨機同步，交接文件裡有說明兩種做法），
照課程 Artifact 上 Stage 6 的每堂課內容，一樣用截圖＋自動點擊的方式邊做邊示範給我看，
做完一堂就回報結果再繼續下一堂。Stage 6 是最後一個 Stage，做完整個 24 堂課程就全部完成了，
完成後幫我更新交接文件標記整個課程完結，並整理一份總結。
```

這段 prompt 的設計重點：

- 第一句強制 Claude 先讀交接文件，不用重新猜專案背景
- 第二句明確給「現在要做哪個 Stage」以及這是最後一個 Stage，避免 Claude 從頭重跑已完成的 Stage 浪費時間
- 第三句涵蓋「新 PC 沒裝過」跟「已裝過」兩種情境，Claude 可以自己判斷分支
- 第四句延續本次 session 已經驗證過的「截圖＋自動點擊＋逐堂回報」demo 節奏，不用重新講一次要怎麼操作
- 最後一句預先交代「這是最後一個 Stage」，完成後直接請 Claude 收尾整理，不用使用者再額外提醒

---

## 第二輪（Pro 素材重做）— 進行中，2026-08-07 家用機 user 帳號

**⚠️ 目前狀態（2026-08-07 這次 session 結束時）**：Stage 1（1.1-1.5）與 Stage 2（2.1-2.3）共 8 堂課已完成並截圖驗證，草稿 `0807-R2`。Stage 3 卡在一個關鍵發現（見下方「Stage 3 卡住的原因」），下一個 session 要先解決這個再繼續 Stage 3-6。因為本次 session 用量超支（週用量 119%，截圖過於頻繁）而提前收工，不是功能卡死。

### 為什麼要重做

2026-08-07 完成第一輪 24 堂課之後，使用者提出：已經升級 CapCut Pro，想把整套課程重做一次，這次刻意挑「有人講話」的 Pro 影片素材，讓第一輪卡住的音訊/字幕類課程（Stage 3）這次能真的測到功能，而不是每次都因為素材沒人聲而改用替代方案。範圍也可以比第一輪更大、不用自我設限太多。

### 使用者的四個明確決定

1. **第一輪完全保留，不刪不改**：草稿 `0806`（B00332）與 `0807`（這台家用機）都留著對照，第二輪另外新建草稿，不覆蓋。
2. **第二輪從 Stage 1 開始**（不重跑 Stage 0）：帳號與 Pro 生效狀態已經在第一輪確認過，直接進 1.1 建立專案。
3. **素材優先用 CapCut 內建資料庫**：在資料庫／熱門分類找**標有 Pro（左上角紫色鑽石）、畫面有人物在說話/訪談/口白類型**的影片，不用另外找外部檔案匯入。
4. **範圍放寬，不用刻意避開 Pro 或複雜功能**：因為 Pro 已生效，遇到第一輪因為「免費帳號要避開 Pro 素材」而繞開的情境，這次可以直接用；第一輪踩到的功能限制（LUT 無內建清單、遮罩轉場圖示無回應）值得重新測一次，確認是版本通病還是跟第一輪那支素材有關。

### 建議的新草稿命名

第二輪在這台機器建新專案，避免跟 `0807` 撞名，建議命名 **`0807-R2`**（或任何不跟現有兩個草稿撞名的名稱，Claude 可自行決定，記得回報實際用的名稱）。

### 第二輪 Stage 1-6 執行重點（對照第一輪，差異處已標註）

| Stage | Lessons | 第一輪做法 | 第二輪這次要注意的差異 |
| --- | --- | --- | --- |
| 1 | 1.1-1.5 建立專案/匯入/時間軸/分割/首次匯出 | 用免費素材，避開 Pro 鑽石標記 | **這次可以直接選 Pro 素材**（有鑽石標記也沒關係）；優先找片頭就有人在講話的素材，為 Stage 3 鋪路 |
| 2 | 2.1-2.3 精準剪輯/素材屬性/多軌子母畫面 | 免費素材 | 沿用同一支或另一支有人聲的 Pro 素材皆可，做法同第一輪 |
| 3 | 3.1-3.4 音訊基礎/節奏踩點/自動字幕/花字貼紙 | ⚠️ 素材完全無語音，擷取音訊/自動字幕全部灰階或無輸出，改用背景音樂+手動文字替代 | **關鍵驗證點**：這次素材有真人講話，預期「擷取音訊」按鈕應該可點、「自動字幕」應該能產生真的逐字稿。務必實際點開驗證，並記錄逐字稿準確度、簡繁轉換是否正確 |
| 4 | 4.1-4.4 特效轉場/濾鏡調節/LUT/蒙版去背 | LUT 無內建清單（只能匯入 .cube）；遮罩轉場的形狀圖示點擊無回應，判斷是版本問題 | 重新測一次 LUT 分頁跟遮罩轉場面板，確認這兩個限制在 Pro 帳號 + 新素材下是否仍然存在（大機率還是版本限制，但務必實測一次不要憑記憶跳過） |
| 5 | 5.1-5.3 關鍵幀原理/進階組合/動態運鏡 | 縮放/位置/透明度關鍵幀 | 做法同第一輪，可視素材內容調整運鏡設計 |
| 6 | 6.1-6.3 命題實作/正式導出/素材管理疑難排解 | 6.1 整合四技巧；6.2 實測 30/60fps 檔案大小幾乎無差；6.3 素材遺失/SRT 字幕/雲端同步三發現 | **6.3 的 SRT 字幕子任務這次應該能拿到真的字幕內容**（第一輪因為無人聲拿到空結果），可以進一步檢查匯出的 SRT 檔案簡繁轉換是否正確，這是第一輪做不到、這次才補得起來的部分 |

### 第二輪進度紀錄（2026-08-07 家用機 user 帳號，本次 session）

**草稿**：`0807-R2`（9:16、30fps），路徑 `C:\Users\user\AppData\Local\CapCut\User Data\Projects\com.lveditor.draft\0807 (1)`（顯示名稱是 `0807-R2`，內部資料夾名稱沿用了 CapCut 自動產生的 `0807 (1)`，不影響使用）。

**Stage 1（1.1-1.5）✅ 完成**：

- 搜尋「man talking to camera」找到素材 **"old senior man talks to camera - green screen - studio"**（40 秒，商業用途，綠幕講者），加入時間軸
- 精準剪輯點：方向鍵逐格移動播放頭（00:00:26:04 → 00:00:26:00，4 格）
- 分割 + 無縫刪除：00:00:26:00 處分割，刪除右段，40:29 → 26:00 秒，右側無縫接合，ripple delete 驗證成功
- 匯出測試：套用「多彩世界」濾鏡（已編輯狀態）後直接匯出成功，**完全沒有跳出 Pro 限制或版權「未編輯」警告**（第一輪的兩個坑這次都繞過了）。匯出面板還多了「以 Ultra HD 匯出」的 Pro 專屬按鈕（第一輪免費帳號看不到）。輸出檔案：`C:\Users\user\AppData\Local\CapCut\Videos\0807-R2.mp4`（38.3MB）

**Stage 2（2.1-2.3）✅ 完成**：

- 2.1 精準剪輯點微調：方向鍵移動 5 格，00:00:06:15 → 00:00:06:20
- 2.2 素材屬性調整：速度 1.00x → 1.50x（時長 26.0s → 17.4s），並開啟「變更音調」保留原始音高（避免加速變聲影響後續字幕辨識，這是第一輪沒機會測到的細節）；套用「漸隱閉幕」退場動畫（0.5s）。**⚠️ 示範完後已把速度復原回 1.00x**，因為 Stage 3 要測試語音辨識，需要乾淨未變速的音訊
- 2.3 多軌子母畫面：搜尋「ocean waves」找到 11.9 秒海浪素材，拖曳到時間軸空白處建新軌（沒有直接點「+」，避免被插入同軌），縮放 40% + Y 位置 -650，確認子母畫面疊加生效（講者為主畫面，海浪疊在中下方）

**Stage 3 卡住的原因（下一個 session 要先解決）**：

- 對主視頻素材「old senior man talks to camera - green screen - studio」右鍵，選單裡「逐字稿」「產生字幕」「擷取音訊」三個選項**視覺上不是灰階**（跟第一輪不同，文字是正常對比度、有紫色 Pro 徽章圖示），但**實際點擊完全沒反應**——選單不會關閉、時間軸不會出現新的音訊軌
- 進一步比對素材屬性面板：這支素材選取後右側只有「影片／速度／插入動畫／調整／AI風格化」5 個分頁，**沒有「音樂」分頁**；對照同專案的海浪素材（有真實海浪聲），選取後有「影片／音樂／速度／插入動畫／調整／AI風格化」6 個分頁，多了「音樂」
- **結論**：這支素材雖然畫面上是真人對鏡頭講話、比手勢，但**影片檔案本身沒有錄到聲音**（是設計給使用者自行配音的靜音 B-roll），不是我們挑錯素材類型，而是這支具體素材剛好無聲。跟第一輪「素材完全無語音」的限制本質相同，但這次是有確切證據（缺少音樂分頁）而不是憑感覺判斷
- **下一步**：session 結束前已在資料庫搜尋「interview response camera」尋找替代素材，還沒選定。下一個 session 要先做的事：
  1. 在 CapCut 資料庫多試幾支「有人講話」的素材，加入時間軸後選取，**檢查右側屬性面板有沒有「音樂」分頁**——有才代表真的有內嵌語音，這是比"畫面看起來像在講話"更可靠的判斷方式
  2. 找到確認有音樂分頁的素材後，可以考慮換掉 Stage 1-2 已用的靜音素材（或在 `0807-R2` 新增一段有聲素材專門用於 Stage 3 測試），重新測「擷取音訊」「自動字幕」「逐字稿」三個功能
  3. 驗證通過後才繼續 Stage 4-6

**本次 session 提前收工原因**：不是功能卡死，是截圖驗證太頻繁導致用量超支（週用量到 119%，session log 40MB），使用者要求先停在這裡，不是任務本身失敗。

### 給下一個 session 的起手 Prompt（直接貼這段）

```
讀 doc/交接文件-capcut-practice.md，接續 CapCut 練習專案的「第二輪」（不是第一輪，
第一輪 24 堂課已完成保留，草稿 0806/0807 不要動）。

第二輪草稿 0807-R2 的 Stage 1-2（共 8 堂課）已經完成，細節見文件裡「第二輪進度紀錄」。
現在卡在 Stage 3：目前用的主視頻素材「old senior man talks to camera - green screen - studio」
畫面上是真人講話，但實測發現它其實是靜音素材（屬性面板沒有「音樂」分頁，右鍵的擷取音訊/
自動字幕/逐字稿點了沒反應）。

請先解決這個問題：
1. 到 CapCut 資料庫重新找一支「真的有語音」的素材（加入時間軸後選取，檢查右側屬性面板
   有沒有「音樂」分頁，這是比畫面看起來像講話更可靠的判斷方式），找到後告訴我實際用哪支
2. 驗證通過後（有音樂分頁）才繼續 Stage 3：3.1-3.4 音訊基礎/節奏踩點/自動字幕/花字貼紙，
   重點是這次要拿到真的擷取音訊結果和自動字幕逐字稿，記錄準確度和簡繁轉換是否正確
3. 接著做完 Stage 4（重測 LUT 和遮罩轉場限制）、Stage 5、Stage 6

⚠️ 用量提醒：上一個 session 因為每步都截圖驗證，用量超支提前收工。這次請減少截圖頻率——
每堂課只在關鍵結果處截圖確認（例如套用完成後看一次），不用每個滑鼠移動或中間步驟都截，
並留意用量提示，超過週配額時主動告知使用者而不是悶頭做完。

做完 Stage 6 後更新交接文件，把「第二輪」章節標記完成，整理含兩輪對照的總結。
```

這段 prompt 的設計重點：

- 開頭先講清楚接續第二輪、Stage 1-2 已完成，避免下個 session 從頭重做
- 明確點出 Stage 3 卡住的具體原因和判斷方法（音樂分頁），不用重新摸索
- 加入用量提醒，這是本次 session 的直接教訓，避免下次又超支提前收工
- 結尾一樣要求截圖示範＋逐堂回報＋收尾更新文件，延續第一輪已驗證有效的協作節奏

---

## 第三輪（公司機 B00332 接續嘗試）— 2026-08-07 同一天，另一個 session，中途停止

**⚠️ 這個章節記錄一次「沒做完就主動停止」的 session，純供參考，不是可以直接接續的完整進度。**

### 為什麼會有第三輪

延續第二輪 Stage 3 卡住的 handoff prompt，在**公司機 B00332** 開新 session 執行「讀 doc/交接文件、接續第二輪 Stage 3」。但一開始就發現關鍵問題：

**第二輪草稿 `0807-R2` 存在家用機（user 帳號），這台公司機（B00332）完全沒有這份草稿**（草稿不跨機同步，這件事交接文件本來就有記錄，但起手 prompt 沒有特別提醒「請先確認你在哪台機器」，導致新 session 一開始沒發現機器不對）。這台機器本機 CapCut 草稿資料夾只有 `0723`、`0806`（第一輪的），CapCut 桌面版已安裝（9.1.0.3879）但當時未執行。

問過使用者後，決定：**在公司機 B00332 上新建「第三輪」草稿**，快速重建 Stage 1 基礎後直接跳去 Stage 3 排查語音素材問題（不強求完整重跑 Stage 1-2 的每個子步驟）。

### 第三輪實際做了什麼

1. 確認本機已安裝 CapCut 桌面版（`C:\Users\B00332\AppData\Local\CapCut\Apps\CapCut.exe`，版本 9.1.0.3879），啟動後確認已登入楊政憲帳號（2820807665），**Pro 續訂日期 2026/08/12 有效**
2. 新建草稿專案：CapCut 預設命名為 `0807`（跟家用機第一輪的 `0807` 同名，但是**不同機器、不同資料夾**，路徑是 `C:/Users/B00332/AppData/Local/CapCut/User Data/Projects/com.lveditor.draft/0807`，不會互相覆蓋或混淆資料，只是閱讀文件時容易搞混，下面統一稱它「B00332-0807」）。**嘗試把它改名成 `0807-R3-B00332` 但失敗**（雙擊標題文字沒有進入可編輯狀態，貼上動作沒有生效，畫面仍顯示 `0807`）——這個改名操作不影響功能，之後有空可以再試從首頁專案清單右鍵重新命名，非急件
3. 專案設定確認：長寬比例「原圖」、影格率 30.00fps
4. 在資料庫搜尋「interview talking camera」，測試第 1 個候選素材 **"News reporter talking in studio."**（14:27 秒，新聞主播棚內素材）：加入時間軸後選取，右側屬性面板只有「影片／速度／插入動畫／調整／AI風格化」5 個分頁，**沒有「音樂」分頁**——跟第二輪卡住的那支素材（"old senior man talks to camera"）是同樣的情況，這支新聞主播素材也是**靜音 B-roll**，沒有真的內嵌語音。已用 Ctrl+Z 復原移除這個素材
5. 開始測試第 2 個候選素材（自拍講話風格、00:11 秒），選取＋準備點擊加入時間軸的「+」按鈕時，**CapCut 視窗被意外縮到最小、焦點被搶走**（screenshot 一度整片黑畫面）——原因判斷：這台公司機同時開很多視窗（多個 Claude Code CLI 分頁、WeChat、小算盤等），桌面自動化用螢幕座標點擊，過程中至少 3 次操作意外命中其他視窗（誤開 CapCut「設定」對話框、誤開「快速鍵」設定對話框、最後這次視窗被縮到最小切到別的 terminal 分頁），每次都要重新截圖確認「現在抓到的真的是 CapCut 嗎」才敢繼續，不是 CapCut 本身故障或素材問題
6. 已用 `AttachThreadInput` + `SetForegroundWindow` 技巧把 CapCut 視窗拉回前景並確認 `IsIconic: 0`（未縮小）、焦點正確，**但這時使用者要求直接停止整個任務**，所以**第 2 個候選素材有沒有成功加入時間軸、有沒有音樂分頁，都還沒驗證，是未完成狀態**

### 第三輪結束時的確切狀態（未完成，供下一個 session 核實用）

- 公司機 B00332 上存在一個新草稿 `0807`（內部路徑 `com.lveditor.draft/0807`），30fps、原圖比例，**時間軸內容不確定**——已知曾經加入過一次「新聞主播」素材後又復原刪除，之後可能有（也可能沒有）第 2 個候選素材殘留在時間軸上，下一個 session 第一件事是打開這個草稿實際看一眼時間軸再判斷
- 第一輪草稿 `0806`（B00332）、第二輪草稿 `0807-R2`（家用機 user 帳號）**都沒有被這次 session 碰過，仍然保持原狀**
- 目前已知「無語音」的素材黑名單（加入這份清單避免重複踩坑）：
  - `old senior man talks to camera - green screen - studio`（第二輪踩到）
  - `News reporter talking in studio.`（第三輪踩到，14:27 秒新聞主播棚內素材）
- 這兩支的共同特徵：都是「攝影棚／專業製作」風格的人像素材（絕大部分內建免版稅素材庫的 studio/新聞棚類型影片可能就是設計給使用者自己配音的無聲 B-roll）。**下一個 session 找候選素材時，可以優先避開「studio」「news」「green screen」這類明顯棚拍關鍵字，改試「vlog」「selfie」「interview response」「street interview」「talking outdoors」這類看起來像真實同步收音的隨手拍風格**，命中真人語音的機率可能更高

### 給下一個 session 的建議（不是強制起手 prompt，因為這次沒做完，情境比較開放）

下一個 session 開始前，**先確認自己在哪台機器**：

```bash
whoami
echo $USERPROFILE
ls "$USERPROFILE/AppData/Local/CapCut/User Data/Projects/com.lveditor.draft/"
```

看本機草稿資料夾有哪些草稿，對照交接文件判斷現在是要接續家用機的 `0807-R2`（如果就在家用機上），還是接續公司機的 `0807`（第三輪，如果在 B00332 上）。兩條線目前都卡在同一個問題（Stage 3 找不到真正有語音的素材），**選其中一條接續即可，不用兩條都做**，避免重複勞動。

技術細節提醒（這次 session 新增的踩坑，補進「給 Claude 的操作要點」章節前先記在這裡）：

- **多視窗桌面環境下，每次操作前務必重新 `EnumWindows` 確認 CapCut 的 hwnd 還在、還在前景，不要沿用舊的 hwnd 變數**——這次至少 3 次因為視窗被其他 app（甚至是自己這個 Claude Code CLI 的其他分頁）搶走焦點或縮到最小，導致截圖/點擊打到錯的視窗
- **CapCut 專案改名（雙擊頂部標題文字）這次沒有成功**，原因未查清，之後可以改試「首頁專案清單 → 右鍵縮圖 → 重新命名」這條路徑
- 找素材時，**先看縮圖旁的秒數/描述文字判斷是不是「studio/news/green screen」這類棚拍關鍵字**，再決定要不要花時間加入時間軸測試音樂分頁，可以省下不少來回

### 本次 session 中止原因

不是功能卡死、不是用量超支，是使用者主動要求「先不用繼續，把目前做的事記下來就好」，直接中止任務。

---

## 第四輪（公司機 B00332，執行練習題腳本）— 2026-08-07 又一個 session，因用量提前收工

### 背景

同一天稍晚，使用者要求把新寫的 `doc/練習題-仿製addwii短片.md`（仿製 addwii 33 秒開箱短片的練習腳本）實際跑一次 live demo。一開始就發現公司機上其實已經存在一個先前 session 沒記錄進文件的草稿 **`0807-R3`**（內部路徑 `C:/Users/B00332/AppData/Local/CapCut/User Data/Projects/com.lveditor.draft/0807 (2)`，30fps、9:16），時間軸已有倒數數字片段、「Mother and her daughter with dog in the park」素材、「STYLE」文字片段——這些內容的來源不明（可能是更早某次未寫進文件的操作），下一個 session 若要用這個草稿，先確認時間軸內容再決定沿用或清空重做。

### 這次的新發現

選取草稿裡的「Mother and her daughter with dog in the park」素材，檢查右側屬性面板分頁列：**只有「影片／速度／插入動畫／調整／AI風格化」5 個分頁，沒有「音樂」分頁** → 確認也是靜音素材。

**這個發現值得更新既有的判斷法則**：先前第二、三輪只驗證過「studio／news／green screen」這類棚拍風格容易無聲，這次證實**連「媽媽女兒公園遛狗」這種居家生活/家庭風格的素材也一樣沒有內嵌語音**。目前已知無聲的素材類型清單（都用同一個「查音樂分頁」方法排除）：

- `old senior man talks to camera - green screen - studio`（第二輪）
- `News reporter talking in studio.`（第三輪）
- `Mother and her daughter with their dog in the park`（第四輪，新增）

**修正後的結論**：畫面內容類型（棚拍 vs 居家 vs 生活風格）完全不能預測有沒有聲音，CapCut 免版稅素材庫裡「有沒有內嵌語音」看起來更接近隨機分佈，不能靠關鍵字直覺篩選，**每一支候選素材都必須實際加入時間軸、選取、檢查屬性面板分頁列**，沒有捷徑。`doc/練習題-仿製addwii短片.md` 裡「避開 studio/news，改用 vlog/selfie」的建議應視為**弱訊號、不是可靠篩選法**，之後再更新該文件澄清這點。

### 中止原因

**用量超支**：本次 session 開始時週用量已在 ~99%，執行到這一步已經逼近上限，使用者尚未下令，Claude 主動判斷比照前兩輪的教訓（因為超支被迫中斷過兩次）提前停止，避免像第二輪一樣悶頭做到被截斷。**這次還沒找到任何一支確認有聲的素材**，Stage 3 / 練習 7.1 的核心問題依然未解。

### 給下一個 session 的建議

1. 先確認用量狀況（若接近週配額重置時間，等重置後再開始這項高截圖量的任務）
2. 打開草稿 `0807-R3`，先看一眼時間軸內容決定是否沿用
3. 繼續在資料庫搜尋候選素材（`interview response`／`street interview`／`talking outdoors`／`vlog` 等關鍵字**僅供搜尋起點，不代表一定有聲**），每支都要選取檢查「音樂」分頁，直到找到一支確認有聲的為止
4. 找到後才繼續 `doc/練習題-仿製addwii短片.md` 的 7.2-7.6

---

## 第五輪（公司機 B00332，解掉 Stage 3 卡點）— 2026-08-07 又一個 session

### 背景

延續 `doc/練習題-仿製addwii短片.md` 的練習題，使用者要求「建立新的 CapCut 專案，逐步 live demo 仿製 addwii 短片的每個步驟，重點是學工具不是成品」。開始前先跟使用者確認用量策略（當時週用量已 ~101%，超過配額），使用者選擇「低截圖量進行」，所以這輪操作刻意壓低驗證截圖頻率（只在關鍵結果處截圖，不逐一動作都截）。

### 新建草稿

沒有沿用任何舊草稿（`0806`/`0807`/`0807-R2`/`0807-R3` 都沒動），在公司機 B00332 新建專案，重新命名為 **`addwii-demo`**（內部資料夾路徑 `C:/Users/B00332/AppData/Local/CapCut/User Data/Projects/com.lveditor.draft/0807 (2)`——CapCut 用遞增數字分配內部資料夾名，跟顯示名稱無關，不是覆蓋舊草稿），長寬比例設為 9:16。

**改名踩坑**：右側「詳細資料」面板的名稱欄位是唯讀顯示，直接點擊/貼上無效；改名要走「專案設定」對話框（點右下角「修改」按鈕開啟）裡的可編輯名稱輸入框，才能真的改到。

### Lesson 7.1 結論：CapCut 免費資料庫素材本質上全無聲，不是關鍵字問題

延續前四輪已經黑名單化的發現，這輪額外測試 5 支不同關鍵字（`interview response`／`talking to camera`／`selfie video diary`／`vlogger talking selfie`）搜到的候選素材，包含畫面內容非常像「真人對鏡頭說話」的片段（例如標題直接寫 *"Portrait young man vlogger look at camera talk make live video conference call"* 的 20 秒片段），**全部只有 5 個屬性分頁（影片／速度／插入動畫／調整／AI風格化），沒有「音樂」分頁**。

**最終結論（可以停止找了）**：CapCut 桌面版免費資料庫（`資料庫` → 搜尋）裡的影片素材，本來就是設計成無聲 B-roll，供使用者自行配音/配樂使用，跟畫面內容、關鍵字、講話風格完全無關。**不用再繼續逐支測試找「有聲素材」**，這條路本身就走不通，需改用其他方式取得語音（見下方 7.4 解法）。

### Lesson 7.4 解法：用「文字轉語音」生成真人語音，解掉整個 Stage 3 卡點

1. 選中 B-roll 素材（用上述 "Portrait young man vlogger..." 20 秒證言風格片段當主視頻）後，新增文字圖層（`文字` 分頁 → `新增文字` → 拖曳/點擊 `+` 加入時間軸），輸入證言腳本：「這台空氣清淨機真的改變了我們家的空氣品質，孩子晚上終於不會半夜咳醒了。」
2. 選取該文字圖層 → 右側屬性面板有專屬的 **「文字轉語音」分頁**（跟「文字／插入動畫／追蹤／AI 虛擬替身」同一排）→ 選語音（試過中文分類下的「沉穩解說」）→ 點「產生語音」
3. **產生成功**：時間軸自動多出一條**真實語音音軌**（有波形圖案），確認 CapCut 的 TTS 是走一般語音合成引擎，產出貨真價實的聲音檔，不是純特效
4. 對這條 TTS 音軌測「自動字幕」（`文字` 分頁 → `自動字幕` → 選語言 → 產生）：**成功產出完整逐字稿**，右側「字幕」面板可看到完整轉寫結果：
   - 第 1 句：「这台空气清净机真的改变了我们家的空气品质 孩子晚上」
   - 第 2 句：「终于不会半夜咳醒了」
   - 逐字稿內容完整、語意正確，跟原始輸入的腳本幾乎一字不差

### 兩個值得記錄的真實發現

1. **自動字幕輸出一律是簡體字，即使輸入的文字腳本和 TTS 來源都是繁體中文**：原始文字圖層打的是「這台空氣清淨機真的改變了我們家的空氣品質，孩子晚上終於不會半夜咳醒了」（繁體），但自動字幕辨識出來變成「这台空气清净机真的改变了我们家的空气品质」（簡體）。這證實了練習題 Lesson 7.4 要驗證的疑慮：**CapCut 自動字幕的簡繁轉換不可靠，示範或正式使用時一定要事後手動校對，不能直接信任辨識結果**。
2. **「所說的語言」下拉選單的選取值容易沒有真的生效**：自動字幕面板的語言下拉選單點選「中文」後，UI 顯示會跳回「英文」（可能是 renderer 沒即時同步當前選取值，或點擊座標剛好落在清單重新排序後的舊選項上），但即使顯示卡在「英文」，實際產生的逐字稿仍然完整且正確辨識出中文內容——**推測 CapCut 背後可能有自動語言偵測機制，不完全依賴這個下拉選單的顯示值**。這是本次沒有查清楚根因、但確認「不影響最終結果」的小怪癖，供未來 session 遇到同樣情況時不用太糾結这個下拉選單的顯示狀態。

### 其他這輪做過的步驟

- **字幕樣式**：選取兩段自動字幕，右側「文字」分頁 → 「泡泡音」子分頁，套用白底黑字風格模板（"My Journey" 樣式），對應練習題 7.4 要求的「白底黑字」字幕框質感
- **背景音樂**：`音樂` 分頁搜尋 `upbeat corporate`，加入「Upbeat Corporate Music」（原長 2:48），用時間軸工具列的「分割」圖示在對齊主視頻結尾處（約 00:20）切開，刪除多餘尾段，音樂長度對齊到與視頻等長（20.22 秒）

### 這輪沒做完的部分（下一個 session 可接續）

- **Lesson 7.2**（開場 Hook，淡入轉場 + 大字文案）：未開始
- **Lesson 7.3**（B-roll 分段剪輯 + 縮放關鍵幀推鏡）：未開始，目前時間軸只有單一段 20 秒證言素材，沒有額外 B-roll 片段
- **Lesson 7.5**（品牌收尾卡，純色背景 + Logo 淡入）：未開始
- **Lesson 7.6 剩餘部分**（音量 ducking、正式匯出 1080×1920 mp4）：背景音樂已加但沒做人聲段落降低音量的 ducking，也還沒實際跑匯出

### 中止原因

**用量守則主動觸發**：本次 session 一開始週用量已達 275MB 配額的 ~101%（超過），這是連續第 4 次在這個練習題系列踩到用量問題（前三輪也都因此中途停止）。這次先跟使用者確認策略（選擇「低截圖量」），做到 Lesson 7.1+7.4 的核心驗證後主動在自然段落收工，把發現寫回文件，不強行做完剩餘 Lesson 冒用量超支風險。

### 給下一個 session 的建議

1. 直接打開草稿 `addwii-demo`（公司機 B00332，路徑見上方），目前狀態：9:16、20.22 秒、含 1 段證言視頻 + TTS 語音 + 已產生並套用樣式的自動字幕 + 已裁切的背景音樂
2. 先看一眼用量狀況再決定要不要繼續（本文件記錄的模式：這個系列的截圖密集任務很容易撞配額）
3. 接續 Lesson 7.2（開場）→ 7.3（B-roll，可以複用前幾輪已知「無聲但畫面 OK」的素材，反正只當畫面用）→ 7.5（收尾卡）→ 7.6 剩餘（ducking + 正式匯出）
4. 若要示範「找有聲素材」的過程，不用再重新搜尋資料庫——直接引用本文件「Lesson 7.1 結論」，改示範 TTS 生成語音的完整流程即可，這是本次驗證過的可靠替代方案

---

## 第六輪（EP1 教學逐步復刻）— 2026-08-08，家用機 user 帳號

### 背景

使用者看完 YouTube 影片《免費影片剪輯軟體！新手也能輕鬆學會用電腦剪片！【剪映教學分享EP1】》（分享家-羽，<https://youtu.be/Ke43EELMDlM> ，146.8萬觀看，見「YouTube 剪映熱門影片 TOP10」查詢結果第一名）後，要求依照影片內容順序在旁邊開著的 CapCut 桌面版逐步操作 live demo。開工前先用 AskUserQuestion 跟使用者確認範圍（考量本系列過去 4 輪都因截圖過密集撞配額），使用者選擇「完整跑一遍全部 12+ 技巧」，並接受可能需要跨 session、進度會寫進交接文件。

### 新建草稿

新建專案，改名為 **`ep1-demo`**（內部路徑 `C:/Users/user/AppData/Local/CapCut/User Data/Projects/com.lveditor.draft/0808`），長寬比例設為 **16:9**（對應影片中「上傳 YouTube 選 16:9」的說明，這跟前五輪慣用的 9:16 不同，因為 EP1 影片主題是電腦版基礎操作而非短影音）。改名走「詳細資料」面板右下角「修改」按鈕開啟的「專案設定」對話框（沿用第五輪已知的正確路徑，非直接雙擊標題列）。

### 完成的技巧（11/13，對照 EP1 影片章節順序）

| # | 技巧 | 實際操作 | 結果 |
| --- | --- | --- | --- |
| 1 | 匯入素材 + 16:9 比例 | 資料庫拖曳 "Green corn plants move in the wind"（14:22）到時間軸；專案設定對話框選 16:9 | ✅ |
| 2 | 分割與無縫刪除 | 播放頭移到 00:00:03:10，Ctrl+B 分割，選前半段按 Delete，右段自動無縫貼齊到 00:00:00 | ✅ |
| 3 | 插入片段 | 資料庫拖曳 "Time Lapse of White fluffy clouds"（20:16）到主軌尾端，自動接續無縫隙 | ✅ |
| 4 | 音量調整＋配樂淡入淡出 | 音樂庫加入 "Cooking Music"，音量調到 -6.0dB，淡入/淡出各設 1.0s（因庫存視頻素材同前五輪一樣無內嵌語音，改用音樂軌示範，做法與第五輪一致） | ✅ |
| 5 | 變速 | 選取素材 1（玉米田）→ 速度分頁 → 標準變速輸入 2.0x，時長 11.4s→5.7s，後續片段自動往前貼齊 | ✅ |
| 6 | 轉場 | 轉場庫 → 基礎分類 → 拖曳「疊化」到兩段影片交界處，跳出「片段太短」提示，確認建立重複影格套用，時長 0.5s | ✅ |
| 7 | 文字＋花字＋動畫 | 新增預設文字，用剪貼簿貼上「剪映教學示範」，套用花字樣式（橘黃色描邊），進場動畫「彈入」、退場動畫「日韓淡出」 | ✅ |
| 8 | 文字轉語音 TTS | 選取文字圖層 → 文字轉語音分頁 → 中文分類選「解說小帥」→ 產生語音，時間軸多出真實語音音軌（含波形） | ✅ |
| 9 | 自動字幕辨識 | 選取 TTS 語音軌 → 自動字幕 → 所說語言選中文 → 產生，成功產出字幕文字圖層，內容與 TTS 腳本一致 | ✅ |
| 10 | B-Roll 疊加軌道 | 資料庫拖曳 "Grilling lobsters"（6:22）到主視頻軌上方的空白區，成功建立新疊加軌道並在時間軸上與主軌重疊；播放頭移入其時間範圍時，預覽畫面正確顯示疊加軌內容蓋過主軌（證實圖層順序生效） | ⚠️ 部分完成（見下方踩坑，縮放/位置調整未能操作） |
| 11 | 匯出設定 | 匯出面板：解析度 1080P／位元率 推薦／編解碼器 H.264／格式 mp4／畫面速率 30fps；Pro 帳號額外顯示「以 Ultra HD 匯出」按鈕；實際點擊匯出，成功產出 `ep1-demo.mp4` | ✅ |

**未做**：EP1 影片中的「照片幻燈片＋轉場＋配樂」段落——CapCut 內建免版稅資料庫以影片素材為主，快速瀏覽「熱門」「背景」分類未找到可直接拖曳的靜態照片素材，權衡時間後略過，未深入找專門的照片分類。下一個 session 若要補這段，可以先查資料庫左側分類清單有沒有更明確的「圖片」入口，或改用「背景」分類裡的靜態色卡/材質圖充當示範用照片。

### 新發現的踩坑：新建疊加軌道左鍵點擊選取失效，右鍵正常

在主視頻軌上方**新建的疊加軌道**（用拖曳素材到空白區域的方式建立）上，對該軌道的影片片段做**左鍵單擊**（不論點縮圖中央、片段標題列、或改用「先點空白處再點片段」等多種方式重試約 10 次）**完全無法選取該片段**——右側屬性面板始終停留在前一個選取對象（主軌上的另一段影片），播放頭也不會因為點擊而移動。但對同一個片段**右鍵**可以正常叫出完整的片段操作選單（拷貝／分割／刪除／編輯等），證實這個片段在資料層面是真實存在、可定址的物件，只是左鍵點擊的 hit-test 在這個特定軌道位置失效。

**驗證方法**：把播放頭移動到疊加片段的時間範圍內（例如用點擊時間軸尺規移動播放頭，而非點擊片段本身），確認預覽畫面正確顯示疊加片段內容蓋過主軌畫面——這證明片段本身運作正常、圖層順序（後蓋前）也正確，純粹是「左鍵選取」這個互動路徑在新建的疊加軌道上失效。

**因應方式**（供下一個 session 遇到同樣狀況參考）：

1. 不要在同一個片段上重試超過 3-4 次左鍵點擊，浪費 session 用量
2. 改用右鍵選單操作（拷貝/貼上屬性、編輯等子選單可能可以達到部分效果，但這次沒有進一步測試每個子選項）
3. 或者：先用左鍵點擊該軌道上的**其他**（例如更早建立、非本次新建疊加軌道的）片段確認選取功能本身正常，藉此排除「全域左鍵失效」的可能，把問題範圍縮小到「這個特定新建軌道」
4. 若急需做子母畫面（PIP）效果的縮放/位置調整，可以嘗試：改用拖曳素材到時間軸空白區時**更精確地對齊到已存在軌道的正上方/正下方**（而非留出全新的一行），或者疊加片段建立後立即（不要做其他操作）馬上嘗試點擊，避開可能的「新軌道初次渲染的 hit-test 尚未就緒」時間窗
5. 這是本輪唯一沒解掉的功能限制，值得下一輪優先重新測試，確認是偶發還是這個 CapCut 版本（在這台家用機上）的通用行為

### 給下一個 session 的建議

1. 直接打開草稿 `ep1-demo`（家用機 user 帳號，路徑見上方），目前狀態：16:9、1分21秒、已完整匯出一次（`C:/Users/user/AppData/Local/CapCut/Videos/ep1-demo.mp4`，121.2MB）
2. 若要補完整個 EP1 系列，剩下兩件事：(a) 照片幻燈片段落（目前完全沒做）(b) B-Roll 疊加片段的縮放/位置調整（受限於上述踩坑，只完成了疊加本身，還沒做子母畫面縮放）
3. 開工前先重新測一次「新建疊加軌道左鍵選取」是否仍然失效，若已正常就直接補完縮放/位置調整；若仍失效，優先嘗試上方「因應方式」列出的替代路徑，避免重複踩坑超過 3-4 次
4. 這輪示範用的素材／設定都記錄在上方表格，接續時不需要重新從頭建立，可以直接在 `ep1-demo` 草稿上继续加工

---

## 第七輪（旁白 TTS 重做版）— 2026-08-08，家用機 user 帳號，緊接第六輪

### 背景

使用者看完第六輪的示範後，要求「同樣的內容重做一次」，但這次每個動作前要先在畫面上顯示文字說明要做什麼、轉成 TTS 語音唸出來，然後才實際操作螢幕，做成一個口白教學的節奏。開工前先用 AskUserQuestion 提醒使用者：這個 session 的 log 已經很肥大（44.7MB）、本週用量已超額 136%，問要不要開新 session；使用者選擇「接受風險，這個 session 全做」。

### 技術作法

每個步驟固定三段式：

1. **文字先寫出來**：用 `print()` 把旁白文字輸出到對話（使用者看得到）
2. **轉 TTS 播放**：`python -m edge_tts --voice zh-TW-HsiaoChenNeural --text "..." --write-media out.mp3`，產生後用 `ffplay -nodisp -autoexit -loglevel quiet out.mp3` 播放——`-nodisp` 是關鍵，不會跳出播放視窗搶走 CapCut 的前景焦點
3. **操作 CapCut**：沿用第六輪已經驗證過的座標和拖曳序列，原封不動重跑一遍

新開草稿 **`ep1-demo-2`**（內部路徑 `C:/Users/user/AppData/Local/CapCut/User Data/Projects/com.lveditor.draft/0808 (1)`），16:9，跟第六輪完全相同的 11 個技巧（匯入/比例、分割刪接、插入、音量+配樂淡入淡出、變速、轉場、文字花字動畫、TTS、自動字幕、B-Roll疊加、匯出設定），逐一先旁白再操作，全部成功重現，最後匯出 `ep1-demo-2.mp4`（121.2MB，跟第六輪的 `ep1-demo.mp4` 檔案大小幾乎一致，證實兩輪操作結果一致）。

### 重要發現：B-Roll 疊加軌道左鍵選取失效是可重現 bug，不是偶發

第六輪只踩到一次，當時無法確定是偶發還是版本通病。這輪**用不同的 B-Roll 素材（"Aerial Top Down View of Aspen Trees in Forest with Yellow Leaves"，跟第六輪的 "Grilling lobsters" 不同）、在新的草稿上重新測一次**，依然是同樣的症狀：

- 拖曳素材到主視頻軌上方空白處，成功建立新疊加軌道，片段本身視覺上有白色選取框（看起來像選取成功）
- 左鍵點擊該片段，右側屬性面板卻仍然停留在「詳細資料」（專案層級資訊），不會切換到影片片段的「基礎」屬性面板
- 播放頭移到疊加片段的時間範圍內，預覽畫面正確顯示疊加片段內容蓋過主軌——證實片段資料本身正常、圖層順序正確，純粹是左鍵選取這個互動路徑失效

**結論**：這是這台家用機上 CapCut 桌面版（這個版本）的**通用行為**，不是特定素材或特定草稿的偶發問題。下一個 session 若要做疊加片段的縮放/位置調整，不用再花時間重新驗證是否偶發，直接參考第六輪文件裡「因應方式」的替代路徑（右鍵選單操作、或嘗試更貼齊既有軌道邊界的拖放位置）。

### 給下一個 session 的建議

1. 兩個草稿都保留：`ep1-demo`（第六輪）、`ep1-demo-2`（第七輪，含旁白流程），內容幾乎一致，差別只在有沒有配旁白操作紀錄
2. 若要繼續加碼旁白教學的形式（例如錄製成一支完整教學影片），這輪驗證過的 edge-tts + ffplay 無視窗播放技術可以直接沿用，不用重新摸索
3. B-Roll 疊加片段的縮放/位置調整依然是兩輪都沒解掉的功能限制，已確認是可重現 bug，下一輪應該直接嘗試「因應方式」列出的替代路徑，而不是再重複驗證是否偶發
4. 這個系列已經連續兩輪在同一個 session 內完成大量操作且用量超支（本輪開工前週用量已 136%），下一個 session 開工前務必先看用量狀況，必要時主動建議使用者開新 session

---

## 第八輪（把旁白文字+TTS 加進影片本身）— 2026-08-08，家用機 user 帳號，緊接第七輪，未完成

### 背景

使用者看完第七輪的旁白 live demo 後，要求把「剛剛示範時講的旁白」整理收集起來，變成畫面上的文字字幕＋中文 TTS 語音，直接加進 `ep1-demo-2` 這個 CapCut 專案裡（不是另開新工具），做成一支「有旁白講解的教學影片」，最後匯出新檔名。開工前用 AskUserQuestion 跟使用者確認了兩個關鍵設計決策：

1. **旁白總長(~2分13秒) vs 畫面長度(1分21秒) 對不上**：使用者選「延長畫面配合旁白」（不是縮短旁白或讓畫面停格）
2. **旁白要不要同時做成畫面字幕**：使用者選「語音+字幕都要」（用文字圖層本身當字幕，配合文字轉語音）

### 旁白文字收集（14 段，來自第七輪 live demo 的旁白 mp3）

14 段文字內容、edge-tts 量測秒數、以及本輪規劃的時間軸位置，都已經用 ffprobe 精確量過（見下方「技術做法」），完整文字列表已經在對話紀錄裡，之後接續時直接引用即可，不用重新翻找。

### 技術做法（已驗證可行的單段流程）

1. **量測旁白總長**：對第七輪留下的 14 個 narr_00.mp3 ~ narr_13.mp3（edge-tts 產生，路徑在 scratchpad 目錄，session 結束會清掉，下次要重新生成或重新輸出逐字稿）跑 ffprobe 量出精確秒數，加總 133.464 秒
2. **延長主視頻配合總長**：選取主視頻兩段素材，速度分頁的「時長」欄位可以直接輸入目標秒數（不用去調速度倍數，CapCut 會自動反推速度）——clip1（玉米田）拉到 70.0s（對應 0.16x 速度），clip2（雲）拉到 65.0s（對應 0.32x 速度），加總剛好 135 秒，專案總長變成 00:02:15:00，蓋過旁白總長
3. **時間軸縮放校準**：先點兩下「-」zoom 縮小，再點兩下「+」放大回來，量出目前縮放下的比例尺（本輪校準結果：起點 x≈204px 對應 t=0，比例尺 22px/秒），之後每段旁白的目標畫面座標 = 204 + 22×時間(秒)
4. **單段旁白新增流程**（已驗證對第 0-3 段有效）：
   - 剪貼簿設定該段文字內容（`Set-Clipboard`）
   - 左側媒體面板：文字 tab → 確認在「新增文字」子分頁（**不是「自動字幕」子分頁，本輪一開始就在這裡踩坑一次**，兩個分頁的畫面配置差很多，點錯地方會拖曳到空氣）→ 拖曳「預設文字」縮圖到時間軸目標起點（會自動磁吸貼齊前一段的結尾，不用算得很準）
   - 點右側「文字」分頁的內容框，兩下點擊進入編輯狀態 → Ctrl+A 全選 → Ctrl+V 貼上剛剛設定好的旁白文字
   - 拖曳這段文字片段的右邊界，從預設寬度（約 3 秒）拉到目標時長（用上面算好的目標秒數換算成 x 座標）——**這一步用比較慢、步數較多的拖曳（30-35 步、每步 0.025 秒間隔）比快速拖曳穩定**
   - 切到右側「文字轉語音」分頁 → 選中文語音角色（本輪用「解說小帥」，但第 3 段之後有一次意外選到「親切女聲」，懷疑是面板還沒完全載入完成就點擊，語音角色不完全一致但不影響功能）→ 點「產生語音」，會在音訊軌自動產生一條對應的語音片段

### 已完成：第 0-3 段（4/14）

逐段（不批次）操作，每段完成後都有截圖驗證，全部成功：

| # | 旁白內容前段 | 目標時長 | 結果 |
| --- | --- | --- | --- |
| 0 | 現在開始重新示範一次剪映教學... | 7.4s | ✅ 文字+TTS 都成功，磁吸對齊 t=0 |
| 1 | 接下來，我要把這個新專案改名為... | 7.9s | ✅ 緊接第0段，無縫貼齊 |
| 2 | 接下來，我要到剪映的內建素材庫... | 7.7s | ✅ 緊接第1段 |
| 3 | 現在示範分割與無縫刪除... | 14.6s | ✅ 用較慢的拖曳參數（30步）解掉一次先前失敗，緊接第2段 |

### 待完成：第 4-13 段（10/14）完整文字＋目標時間點

**座標校準參考值**（本輪校準結果，下個 session 開工前務必用「先點兩下『-』縮小、再點兩下『+』放大回來」重新校準一次，不要照抄，只是給個參考起點）：時間軸起點 x≈204px 對應 t=0，比例尺約 22px/秒，換算公式 `x = 204 + 22 × 秒數`。y 座標：新增文字軌拖放用 715（軌道內），拖曳片段右邊界用 707（片段本體列）。

| # | 旁白文字（完整） | 累積開始秒數 | 累積結束秒數 |
| --- | --- | ---: | ---: |
| 4 | 接下來示範插入片段。我再拖曳一段雲的縮時影片，接在第一段的後面。 | 37.5 | 45.1 |
| 5 | 現在示範音量調整跟配樂淡入淡出。我先加入一段背景音樂，然後把音量調到負六分貝，淡入淡出各設定一秒。 | 45.1 | 56.2 |
| 6 | 接著示範變速功能。我選取第一段素材，把速度調成兩倍速，畫面時長會自動縮短一半。 | 56.2 | 65.7 |
| 7 | 現在示範轉場效果。我到轉場庫的基礎分類，把疊化效果拖曳到兩段影片的交界處。 | 65.7 | 74.2 |
| 8 | 接下來加入文字。我新增一段預設文字，改成剪映教學示範，套用花字樣式，再加上進場跟退場動畫。 | 74.2 | 84.9 |
| 9 | 現在示範文字轉語音功能。我選取剛剛的文字圖層，選擇一個中文配音角色，產生真人語音。 | 84.9 | 94.8 |
| 10 | 再來示範自動字幕辨識。我選取剛剛產生的語音音軌，語言選中文，讓系統自動辨識產生字幕。 | 94.8 | 104.9 |
| 11 | 接下來示範 B-Roll 疊加畫面。我從素材庫拖一段新的影片，疊加到主視頻軌的上方，做出畫面疊層的效果。 | 104.9 | 115.5 |
| 12 | 最後一步，示範匯出設定。解析度選 1080P，編解碼器選 H 點 264，格式選 mp4，畫面速率 30fps，然後按下匯出。 | 115.5 | 129.0 |
| 13 | 整個示範流程完成，影片已經成功匯出。 | 129.0 | 133.5 |

第 3 段結尾（約 t=37.5s）跟第 4 段開頭銜接，時間軸上應該無縫。**這份秒數是用 edge-tts 量測的估計值，CapCut 自己的「解說小帥」語音實際生成的音訊通常會比這個估計值短一點**（第 0 段實測：文字目標 7.4s，實際語音只有約 5.5s），屬正常現象，代表文字片段結尾跟下一段開頭之間會有一小段安靜的空白，不是錯誤；但如果某段語音反而比目標長，CapCut 可能會把該段語音自動放到新的一條音訊軌（因為跟下一段時間重疊），這也不是錯誤，純粹是版面問題，不用特別去修。

### 兩次批次失敗（第 4-9 段規劃批次 3 段一組，兩次都全軍覆沒）

把上面「單段流程」寫成迴圈、一次跑 3 段（不逐段截圖驗證，想節省用量），連續兩次都在批次執行到一半時，畫面意外跳出「專案設定」對話框（跟點擊「詳細資料」面板右下角「修改」按鈕的彈窗一模一樣）。研判原因：

- 「文字轉語音」分頁的「產生語音」按鈕座標，跟「詳細資料」面板的「修改」按鈕座標**剛好是同一個位置**（約 1871,563）——這兩個面板不會同時出現，但如果前一步驟（拖曳、貼上內容、拖曳邊界任一步）沒有真正成功，導致右側面板意外退回「詳細資料」view，那麼在這個座標點下去就會誤觸「修改」而不是「產生語音」
- 兩次批次失敗後檢查時間軸，**新的文字/語音片段完全沒有被建立**（不是建立了但內容跑掉，是整批 3 段都沒生效），代表失敗發生在批次的第一段甚至更早，可能是連續執行大量操作後 CapCut 或桌面自動化的時序配合開始不穩定（點擊間隔、面板載入時間在 session 跑了很久之後可能需要抓更長的等待時間）

**因應方式（下一個 session 直接照做，不用重新排查）**：

1. **不要批次處理多段**，即使會拖慢速度也要逐段跑（新增→內容→拖曳邊界→切 TTS 分頁→產生語音），每段之間可以不截圖（省用量），但**每 2-3 段截一次圖驗證**，及早抓到失敗，不要像本輪批次 3 段一次全部盲跑
2. 每個步驟之間的 `time.sleep()` 建議比本輪最後用的參數（0.3-0.9秒）再拉長一點，尤其是「切換文字 tab」「切換文字轉語音 tab」這種需要重新載入面板內容的步驟
3. 如果不小心又跳出「專案設定」對話框，點左下角「取消」（座標約 1089,818）關閉，不要點「儲存」，然後重新檢查時間軸目前實際狀態（哪些段落真的建立了）再繼續，不要假設批次清單裡「應該」都做完了

### 目前 `ep1-demo-2` 專案實際狀態（下一個 session 第一件事：打開草稿核實這個狀態還準不準）

- 長寬比例 16:9，專案總長 00:02:15:00（135 秒，已延長完成，不用再調）
- 主視頻軌：clip1（玉米田，0.16x 速度，70.0s）+ clip2（雲，0.32x 速度，65.0s），無縫接續，**這部分已經完成，不用動**
- 新增了一條「旁白文字」軌道，目前有第 0-3 段（4 段），對應音訊軌上也有 4 段 TTS 語音（第 3 段語音角色跟前面不一致，是「親切女聲」不是「解說小帥」，可以之後選取該片段重新選語音角色再產生一次，非必要）
- 第 4-13 段（10 段）都還沒開始，時間軸從第 3 段結尾（約 00:37）之後到第 13 段結尾（約 00:02:13）都還是空的，只有底下延長過的玉米田/雲畫面+原有的 Cooking Music 背景音樂+B-Roll疊加+原始示範用的「剪映教學示範」文字（這些都是第六/七輪留下的內容，本輪沒有動過，不用管）
- **還沒有匯出任何新檔案**，`ep1-demo-2.mp4`（第七輪的舊匯出）還在，沒有被覆蓋

### 給下一個 session 的建議

1. 先確認用量狀況（這輪結束時 session 已經非常肥大），必要時等配額重置或開新 session
2. 打開 `ep1-demo-2` 草稿，核對上面「目前實際狀態」是否還準確（草稿有自動儲存，本輪最後一次自動儲存時間戳是 05:15，所有動作都應該已經存檔）
3. 接續第 4-13 段（10 段），文字內容跟目標時間軸位置都在上面表格/本文件邏輯裡有跡可循，也可以直接參考第七輪 live demo 對話紀錄裡的旁白原文
4. **逐段做，不要批次**，每 2-3 段驗證一次，比本輪学到的教訓更保守一點沒關係，避免重蹈覆轍
5. 全部 14 段完成後，確認背景音樂 Cooking Music 音量是否需要再調低（避免蓋過旁白人聲，本輪還沒做這步），最後匯出新檔名（例如 `ep1-demo-2-narrated.mp4`），不要覆蓋原本的 `ep1-demo-2.mp4`

### 中止原因

**自動化可靠度下降＋用量守則**：本輪一開始週用量就已經 136% 超額，這輪又新增大量桌面自動化操作（上百次滑鼠鍵盤動作＋約 20 張截圖）。批次處理連續兩次意外失敗（跳出非預期對話框），判斷繼續盲目重試風險大於效益，主動停在完成 4/14 段的自然檢查點，把已驗證可行的單段流程和兩次失敗的具體發現都記錄下來，避免下個 session 重複踩同樣的坑。
